# Artic Engine 
A simple 2d engine written in C++ on SDL3.

### Build
You need to have SDL3 and SDL3_image for building the engine.

## Test Scene

A built‑in test scene is included to demonstrate the core functionality of the engine.  
This scene uses a system of *chunks* — independent units of content or objects managed within the same rendering context — to showcase rendering and input handling.
